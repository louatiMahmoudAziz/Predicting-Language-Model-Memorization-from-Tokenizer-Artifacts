# Core
pyyaml>=6.0
numpy>=1.24
pandas>=2.0
pyarrow>=14.0              # Parquet I/O for features and labels
scikit-learn>=1.3

# Tokenizers
transformers>=4.36
tokenizers>=0.15
sentencepiece>=0.1.99
protobuf>=3.20            # for reading SP model proto (normalizer_id in metadata)

# Language modeling (transformer from scratch)
torch>=2.1

# Optional baseline (off by default in config)
# kenlm>=0.0.0

# Experiment tracking
tqdm>=4.66

# Notebooks
jupyter>=1.0
matplotlib>=3.7
seaborn>=0.12

# Dev / testing
pytest>=7.4
