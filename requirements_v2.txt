# Pin core numerics so SciPy / sklearn match NumPy (avoids numpy._typing / ABI breaks on HPC)
numpy>=1.24.0,<2.3.0
scipy>=1.10.0,<1.16.0
pandas>=2.0.0
scikit-learn>=1.3.0
datasets>=2.14.0
transformers>=4.36.0
torch
shap
pyarrow
